{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the editor window is allowed to reach. The file dialogs are the app's whole I/O surface; the drawing itself never touches the filesystem.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:event:default",
    "dialog:default",
    "opener:default"
  ]
}
